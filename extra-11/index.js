const diâmetro = 2;
let volume = 0, raio = diâmetro / 2;
volume = (4/3) * Math.PI * Math.pow(raio, 3);
console.log(`O volume da esfera é de aproximadamente ${volume.toFixed(2)} unidade de medida cúbica`);